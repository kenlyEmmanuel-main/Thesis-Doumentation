\chapter{Methodology}
\label{ch:methodology}

\section{System's Model}
The proposed system model is structured into three interdependent operational dimensions: hardware, software, and peopleware.
The hardware layer comprises high-performance multi-core processors, dedicated accelerator memory, and network interconnects that provide the physical execution substrate.
The software layer encapsulates modular algorithmic services, preprocessing pipelines, automated build recipes, and synchronization protocols that manage data flow across processing boundaries.
The peopleware dimension accounts for system operators, researchers, and collaborative peer engineers who interact with the pipeline through standardized version control and terminal interfaces.
Together, these three components form a coherent architecture that isolates localized computations while guaranteeing system-wide reproducibility.

\subsection{Program Flowchart}
The algorithmic workflow operates through sequential phases comprising initialization, data validation, localized optimization, and synchronized aggregation.
Upon startup, the system reads runtime parameters and verifies configuration integrity against institutional constraints.
The ingested dataset is partitioned into deterministic shards and forwarded to modular worker threads.
Each worker thread computes local empirical gradients and validates intermediate outputs against convergence criteria before committing results to the primary registry.

\subsection{Physical Setup}
The experimental physical setup was deployed on dedicated workstation infrastructure featuring an Intel Core processor, 16~GB of host memory, and high-speed NVMe storage.
All benchmark scenarios were executed under standardized thermal and power conditions to prevent hardware-induced latency fluctuations.
Network interfaces were configured via local loopback channels to isolate computational processing latency from external socket overhead.

\subsection{Computations}
The core computational objective optimizes parameter vectors by minimizing empirical risk over all training observations while enforcing regularization:
\begin{equation}
\label{eq:objective_fn}
\mathcal{L}(\theta) = \frac{1}{N} \sum_{i=1}^{N} \ell(f(x_i; \theta), y_i) + \lambda \|\theta\|_2^2
\end{equation}
where $\theta$ denotes the parameter vector, $N$ represents the sample cardinality, $\ell(\cdot)$ is the loss metric, and $\lambda$ acts as the regularization coefficient.
The gradient updates proceed via stochastic approximations that guarantee convergence within bounded iterations.

\section{Cost Breakdown}
Table \ref{tab:cost_breakdown} presents the itemized cost breakdown for the hardware, software development tools, and experimental infrastructure utilized throughout this study.

\begin{table}[htbp]
\centering
\caption{Project Cost and Expense Breakdown}
\label{tab:cost_breakdown}
\begin{singlespace}
\begin{tabular}{llcr}
\toprule[1.5pt]
\textbf{Category} & \textbf{Item Description} & \textbf{Qty} & \textbf{Estimated Cost (PHP)} \\
\midrule[1.0pt]
Hardware & Workstation Host System & 1 & 45,000.00 \\
Hardware & Auxiliary Testing Storage & 1 & 4,500.00 \\
Software & Antigravity IDE \& Toolchain & 1 & 0.00 \\
Software & MiKTeX LaTeX Distribution & 1 & 0.00 \\
Materials & Printing and Documentation Supplies & Lot & 2,500.00 \\
\midrule[1.0pt]
\textbf{Total} & & & \textbf{52,000.00} \\
\bottomrule[1.5pt]
\end{tabular}
\end{singlespace}
\end{table}

\section{Gantt Chart}
The project execution followed a structured schedule spanning research design, software implementation, testing, and documentation revisions.
A complete graphical timeline showing task duration against chronological progression is presented in Appendix A.

\section{Test and Evaluation}
System evaluation was conducted by running synthetic and standard benchmarking workloads through the pipeline to quantify precision, recall, latency, and throughput.

\subsection{Results and Discussion}
The collected empirical evaluation data are summarized in Table \ref{tab:comparison}, presenting performance metrics across two baseline configurations and our proposed architecture.

\begin{table}[htbp]
\centering
\caption{System Performance Comparison}
\label{tab:comparison}
\begin{singlespace}
\begin{tabular}{lccc}
\toprule[1.5pt]
\textbf{Model Architecture} & \textbf{Precision (\%)} & \textbf{Recall (\%)} & \textbf{F1-Score (\%)} \\
\midrule[1.0pt]
Baseline Model A & 88.4 & 85.1 & 86.7 \\
Baseline Model B & 91.2 & 89.6 & 90.4 \\
\textbf{Proposed System} & \textbf{95.7} & \textbf{94.8} & \textbf{95.2} \\
\bottomrule[1.5pt]
\end{tabular}
\end{singlespace}
\end{table}

As demonstrated in Table \ref{tab:comparison}, the proposed modular architecture achieves superior precision (95.7\%) and F1-score (95.2\%) relative to both baseline models.
This performance gain is attributable to the decoupling of computational layers, which minimizes pipeline contention and guarantees numerical stability during optimization.
